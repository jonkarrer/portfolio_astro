<div class="relative">
  <svg
    class="absolute top-0 left-0 right-0 bottom-0 w-24 m-auto"
    width="100"
    height="100"
    viewBox="0 0 100 100"
  >
    <circle cx="50" cy="50" r="25"></circle>
  </svg>
  <svg
    class="absolute top-0 left-0 right-0 bottom-0 w-24 m-auto"
    width="80"
    height="80"
    viewBox="0 0 80 80"
  >
    <circle cx="40" cy="40" r="20"></circle>
  </svg>
  <svg
    class="absolute top-0 left-0 right-0 bottom-0 w-24 m-auto"
    width="60"
    height="60"
    viewBox="0 0 60 60"
  >
    <circle cx="30" cy="30" r="15"></circle>
  </svg>
</div>

<style>
  @keyframes drawSpiral {
    50% {
      stroke-dashoffset: 0;
    }
    100% {
      stroke-dashoffset: -158;
    }
  }
  circle {
    fill: none;
    stroke: var(--dark100);
    stroke-width: 2;
    stroke-dasharray: 158;
    stroke-dashoffset: 158;
    animation: drawSpiral 2s linear infinite;
  }
</style>
